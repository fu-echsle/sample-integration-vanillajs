export class LinkItem {
    constructor(_id, _text, _link) {
        this.IsLinkItem = true;
        this.Id = _id;
        this.Text = _text;
        this.Link = _link;
    }
}
export class TextItem {
    constructor(_id, _text, _items) {
        this.IsLinkItem = false;
        this.Id = _id;
        this.PhraseId = _id;
        this.Text = _text;
        this.Items = _items;
    }
}
export const hasLinkItemChild = (item) => {
    if (item.Items) {
        for (let index = 0; index < item.Items.length; index++) {
            if (item.Items[index].IsLinkItem) {
                return true;
            }
        }
    }
    return false;
};
