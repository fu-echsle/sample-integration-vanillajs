import { BasicQueryStringUtils } from '@openid/appauth/built/query_string_utils';
export class NoHashQueryStringUtils extends BasicQueryStringUtils {
    constructor(code, state, error) {
        super();
        this.code = code;
        this.state = state;
        this.error = error;
    }
    parse(_query, _useHash) {
        return {
            code: this.code,
            state: this.state,
            error: this.error
        };
    }
}
