import { JSX } from '../../stencil-public-runtime';
import { Series } from '../entities/series';
export declare class MobileCarSeriesList {
    el: HTMLPhnMobileCarSeriesListElement;
    carSeries: Series[];
    setActiveDrawer: (el: HTMLElement, level: number) => void;
    closeActiveDrawer: (level: number) => void;
    hideAll(): Promise<void>;
    componentDidLoad(): void;
    sendNavigationClickAnalyticsEvent: (series: Series) => void;
    private showCarSubnavigation;
    private hideAllCarSubnavigations;
    render(): JSX.Element;
}
