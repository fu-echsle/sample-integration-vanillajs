/**
 * TODO: Cache is shared between tests and never cleared. Evaluate solution.
 * */
import { IconName } from '../../../types';
export declare const DEFAULT_ICON_NAME: IconName;
export declare const getSvgContent: (url: string) => Promise<string>;
export declare const buildIconUrl: (iconNameOrSource?: string) => string;
