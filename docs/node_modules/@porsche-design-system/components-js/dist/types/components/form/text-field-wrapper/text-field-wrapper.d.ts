import { JSX } from '../../../stencil-public-runtime';
import { BreakpointCustomizable } from '../../../utils';
import { FormState } from '../../../types';
export declare class TextFieldWrapper {
    host: HTMLElement;
    /** The label text. */
    label?: string;
    /** The description text. */
    description?: string;
    /** The validation state. */
    state?: FormState;
    /** The message styled depending on validation state. */
    message?: string;
    /** Show or hide label and description text. For better accessibility it is recommended to show the label. */
    hideLabel?: BreakpointCustomizable<boolean>;
    private disabled;
    private readonly;
    private showPassword;
    private input;
    private searchButtonType;
    private isPasswordToggleable;
    private isInputTypeSearch;
    componentWillLoad(): void;
    componentDidUpdate(): void;
    render(): JSX.Element;
    private get isLabelVisible();
    private get isDescriptionVisible();
    private get isMessageDefined();
    private get isMessageVisible();
    private setInput;
    private setAriaAttributes;
    private setState;
    private labelClick;
    private bindStateListener;
    private updatePasswordToggleable;
    private togglePassword;
    private initInputTypeSearch;
    private onSubmitHandler;
    private addSlottedStyles;
}
