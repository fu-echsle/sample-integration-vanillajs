import { BasicQueryStringUtils } from '@openid/appauth/built/query_string_utils';
import { LocationLike, StringMap } from '@openid/appauth';
export declare class NoHashQueryStringUtils extends BasicQueryStringUtils {
    private code;
    private state;
    private error;
    constructor(code: string, state: string, error: string);
    parse(_query: LocationLike, _useHash?: boolean): StringMap;
}
