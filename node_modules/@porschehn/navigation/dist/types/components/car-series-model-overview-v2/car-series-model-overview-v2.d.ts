import { Series } from '../../entities/series';
interface Loggable {
    triggerDesktopAnalyticsEvent(event: CustomEvent<string>): void;
}
export declare class CarSeriesModelOverviewV2 implements Loggable {
    isMobile: boolean;
    series: Series;
    isDesktop: boolean;
    resizeHandler(): void;
    triggerDesktopAnalyticsEvent(event: CustomEvent<string>): void;
    componentWillLoad(): void;
    render(): any;
}
export {};
