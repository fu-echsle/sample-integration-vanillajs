import { ContentItem } from '../../services/state-tunnel';
export declare class CarFurtherLinks {
    items: ContentItem[];
    sendNavigationClickAnalyticsEvent: (contentItem: ContentItem) => void;
    renderItem(item: ContentItem): any;
    render(): any;
}
