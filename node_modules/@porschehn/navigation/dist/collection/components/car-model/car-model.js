import { Component, Prop, getAssetPath, h } from '@stencil/core';
import { config } from '../../env-config';
import { navContentStore } from '../../state/nav-content-store';
export class CarModel {
    render() {
        const path = getAssetPath(`${config(navContentStore.get('env')).ASSETS_URL}/model-assets`);
        const imageFileEnding = 'jpg';
        const imageType = 'jpeg';
        return (h("a", { class: "car-model", href: this.link },
            h("picture", { class: "model-pic", "aria-hidden": "true" },
                h("source", { type: `image/${imageType}`, media: "(min-width: 1760px)", srcSet: `${path}/1920/${this.image}.${imageFileEnding}, ${path}/1920/${this.image}@2x.${imageFileEnding} 2x` }),
                h("source", { type: `image/${imageType}`, media: "(min-width: 1300px)", srcSet: `${path}/1366/${this.image}.${imageFileEnding}, ${path}/1366/${this.image}@2x.${imageFileEnding} 2x` }),
                h("source", { type: `image/${imageType}`, media: "(min-width: 1000px)", srcSet: `${path}/1024/${this.image}.${imageFileEnding}, ${path}/1024/${this.image}@2x.${imageFileEnding} 2x` }),
                h("source", { type: `image/${imageType}`, media: "(min-width: 760px)", srcSet: `${path}/768/${this.image}.${imageFileEnding}, ${path}/768/${this.image}@2x.${imageFileEnding} 2x` }),
                h("source", { type: `image/${imageType}`, media: "(min-width: 375px)", srcSet: `${path}/375/${this.image}.${imageFileEnding}, ${path}/375/${this.image}@2x.${imageFileEnding} 2x` }),
                h("source", { type: `image/${imageType}`, media: "(min-width: 0px)", srcSet: `${path}/320/${this.image}.${imageFileEnding}, ${path}/320/${this.image}@2x.${imageFileEnding} 2x` }),
                h("img", { alt: this.text })),
            h("div", { class: "model-text" }, this.text)));
    }
    static get is() { return "phn-car-model"; }
    static get encapsulation() { return "scoped"; }
    static get originalStyleUrls() { return {
        "$": ["car-model.scss"]
    }; }
    static get styleUrls() { return {
        "$": ["car-model.css"]
    }; }
    static get assetsDirs() { return ["model-assets"]; }
    static get properties() { return {
        "text": {
            "type": "string",
            "mutable": false,
            "complexType": {
                "original": "string",
                "resolved": "string",
                "references": {}
            },
            "required": false,
            "optional": false,
            "docs": {
                "tags": [],
                "text": ""
            },
            "attribute": "text",
            "reflect": true
        },
        "image": {
            "type": "string",
            "mutable": false,
            "complexType": {
                "original": "string",
                "resolved": "string",
                "references": {}
            },
            "required": false,
            "optional": false,
            "docs": {
                "tags": [],
                "text": ""
            },
            "attribute": "image",
            "reflect": true
        },
        "link": {
            "type": "string",
            "mutable": false,
            "complexType": {
                "original": "string",
                "resolved": "string",
                "references": {}
            },
            "required": false,
            "optional": false,
            "docs": {
                "tags": [],
                "text": ""
            },
            "attribute": "link",
            "reflect": false
        }
    }; }
}
