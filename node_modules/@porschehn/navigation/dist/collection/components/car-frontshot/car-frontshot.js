import { Component, Prop, getAssetPath, h } from '@stencil/core';
import { config } from '../../env-config';
import { navContentStore } from '../../state/nav-content-store';
export class CarFrontshot {
    render() {
        const path = getAssetPath(`${config(navContentStore.get('env')).ASSETS_URL}/frontshot-assets`);
        const imageFileEnding = 'jpg';
        const imageType = 'jpeg';
        return (h("picture", { "aria-hidden": "true" },
            h("source", { type: `image/${imageType}`, media: "(min-width: 1760px)", srcSet: `${path}/1920/${this.image}.${imageFileEnding}, ${path}/1920/${this.image}@2x.${imageFileEnding} 2x` }),
            h("source", { type: `image/${imageType}`, media: "(min-width: 1024px)", srcSet: `${path}/1366/${this.image}.${imageFileEnding}, ${path}/1366/${this.image}@2x.${imageFileEnding} 2x` }),
            h("source", { type: `image/${imageType}`, media: "(min-width: 768px)", srcSet: `${path}/1920/${this.image}.${imageFileEnding}, ${path}/1920/${this.image}@2x.${imageFileEnding} 2x` }),
            h("source", { type: `image/${imageType}`, media: "(min-width: 0px)", srcSet: `${path}/375/${this.image}.${imageFileEnding}, ${path}/375/${this.image}@2x.${imageFileEnding} 2x` }),
            h("img", { src: `${path}/375/${this.image}.${imageFileEnding}`, class: "frontshot", alt: this.text })));
    }
    static get is() { return "phn-car-frontshot"; }
    static get encapsulation() { return "scoped"; }
    static get originalStyleUrls() { return {
        "$": ["car-frontshot.scss"]
    }; }
    static get styleUrls() { return {
        "$": ["car-frontshot.css"]
    }; }
    static get assetsDirs() { return ["frontshot-assets"]; }
    static get properties() { return {
        "text": {
            "type": "string",
            "mutable": false,
            "complexType": {
                "original": "string",
                "resolved": "string",
                "references": {}
            },
            "required": false,
            "optional": false,
            "docs": {
                "tags": [],
                "text": ""
            },
            "attribute": "text",
            "reflect": true
        },
        "image": {
            "type": "string",
            "mutable": false,
            "complexType": {
                "original": "string",
                "resolved": "string",
                "references": {}
            },
            "required": false,
            "optional": false,
            "docs": {
                "tags": [],
                "text": ""
            },
            "attribute": "image",
            "reflect": true
        }
    }; }
}
