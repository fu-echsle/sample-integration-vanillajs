import { createStore } from '@stencil/store';
import { ANALYTICS_APP_ID, DEFAULT_LOCALE } from '../constants';
import { Locale } from '../../components/entities/locale';
export const analyticsSetupStore = createStore({
    applicationId: ANALYTICS_APP_ID,
    locale: new Locale(DEFAULT_LOCALE),
    window: undefined
});
