/* istanbul ignore file */
import { h } from '@stencil/core';
import { createProviderConsumer } from '@stencil/state-tunnel';
import { DEFAULT_ENVIRONMENT, DEFAULT_MENU, DEFAULT_METAFUNCTIONALITIES, DEFAULT_MORE, DEFAULT_SERIES_LIST, DEFAULT_SHOPS } from './constants';
export default createProviderConsumer({
    seriesList: DEFAULT_SERIES_LIST,
    shops: DEFAULT_SHOPS,
    more: DEFAULT_MORE,
    metafunctionalities: DEFAULT_METAFUNCTIONALITIES,
    menu: DEFAULT_MENU,
    env: DEFAULT_ENVIRONMENT,
    loggedInState: undefined
}, (subscribe, child) => h("context-consumer", { subscribe: subscribe, renderer: child }));
