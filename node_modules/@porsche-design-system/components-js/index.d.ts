export declare const PDS_CDN_KEY = "porscheDesignSystemCdn";
/**
 * @property prefix - the prefix used for the components
 * @property cdn - which CDN to use, defaults to 'auto'
 */
export declare type LoadOptions = {
    prefix?: string;
};
export declare const load: ({ prefix }?: LoadOptions) => void;
