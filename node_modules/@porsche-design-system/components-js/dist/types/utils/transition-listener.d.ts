/**
 * Listens to the end of a CSS transition and calls a throttled callback and calls
 * the callback once initially.
 * @param tag
 * @param transitionProperty
 * @param callback
 * @returns void
 */
export declare const transitionListener: (tag: HTMLElement, transitionProperty: string, callback: () => void) => void;
