import { JSX } from '../../../stencil-public-runtime';
import { BreakpointCustomizable } from '../../../utils';
import { FormState } from '../../../types';
export declare class SelectWrapper {
    host: HTMLElement;
    /** The label text. */
    label?: string;
    /** The description text. */
    description?: string;
    /** The validation state. */
    state?: FormState;
    /** The message styled depending on validation state. */
    message?: string;
    /** Show or hide label. For better accessibility it is recommended to show the label. */
    hideLabel?: BreakpointCustomizable<boolean>;
    private disabled;
    private fakeOptionListHidden;
    private optionSelected;
    private optionHighlighted;
    private optionDisabled;
    private isTouch;
    private select;
    private options;
    private fakeOptionListNode;
    private fakeOptionHighlightedNode;
    private selectObserver;
    componentWillLoad(): void;
    componentDidUpdate(): void;
    componentDidUnload(): void;
    render(): JSX.Element;
    private get isLabelVisible();
    private get isDescriptionVisible();
    private get isMessageDefined();
    private get isMessageVisible();
    private initSelect;
    private setAriaAttributes;
    private setState;
    private labelClick;
    private bindStateListener;
    private observeSelect;
    private handleClickOutside;
    private handleMouseEvents;
    private handleKeyboardEvents;
    private setOptionList;
    private setOptionsDisabled;
    private setOptionSelected;
    private createFakeOptionList;
    private cycleFakeOptionList;
    private handleScroll;
    private searchOptions;
    private addSlottedStyles;
}
