import { JSX } from '../../../../stencil-public-runtime';
export declare class TextListItem {
    host: HTMLElement;
    componentDidLoad(): void;
    render(): JSX.Element;
    private get typeOfList();
    private get typeOfOrderedList();
    private get isNestedList();
    private addSlottedStyles;
}
