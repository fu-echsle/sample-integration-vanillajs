'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

const breakpointCustomizable = require('./breakpointCustomizable-2c3f2e06.js');
const index = require('./index-638bfbea.js');
const index$1 = require('./index-7a33061a.js');

const marqueCss = ":host{display:-ms-inline-flexbox;display:inline-flex;vertical-align:top}.p-marque{display:block;width:100px;height:60px}@media (min-width: 1300px){.p-marque{width:120px;height:72px}}.p-marque>img{display:block;width:100%;height:auto}";

class Marque {
    constructor(hostRef) {
        breakpointCustomizable.registerInstance(this, hostRef);
        /**
         * Show/hide trademark sign.
         */
        this.trademark = true;
    }
    render() {
        const cdnBaseUrl =  index$1.MARQUES_CDN_BASE_URL ;
        const manifestPath = index$1.MARQUES_MANIFEST$1[`porscheMarque${this.trademark ? 'Trademark' : ''}`];
        const buildSrcSet = (size) => Object.entries(manifestPath[size])
            .map(([resolution, fileName]) => `${cdnBaseUrl}/${fileName} ${resolution}`)
            .join(',');
        const marqueClasses = index.classnames(breakpointCustomizable.prefix('marque'));
        return (breakpointCustomizable.h("picture", { class: marqueClasses }, breakpointCustomizable.h("source", { srcSet: buildSrcSet('medium'), media: "(min-width: 1300px)" }), breakpointCustomizable.h("source", { srcSet: buildSrcSet('small') }), breakpointCustomizable.h("img", { src: `${cdnBaseUrl}/${manifestPath.medium['2x']}`, alt: "Porsche" })));
    }
}
Marque.style = marqueCss;

exports.p_marque = Marque;
