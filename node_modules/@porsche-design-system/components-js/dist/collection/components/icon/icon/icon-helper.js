export const isUrl = (str) => (str === null || str === void 0 ? void 0 : str.length) > 0 && /(\/)/.test(str);
// eslint-disable-next-line @typescript-eslint/explicit-module-boundary-types
export const isStr = (val) => typeof val === 'string';
