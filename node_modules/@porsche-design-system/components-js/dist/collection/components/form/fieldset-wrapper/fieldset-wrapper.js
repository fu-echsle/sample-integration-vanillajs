import { Component, Prop, h } from '@stencil/core';
import cx from 'classnames';
import { prefix } from '../../../utils';
export class FieldsetWrapper {
    constructor() {
        /** The label text. */
        this.label = '';
    }
    render() {
        const fieldsetClasses = cx(prefix('fieldset-wrapper'));
        const labelClasses = cx(prefix('fieldset-wrapper__label'));
        return (h("fieldset", { class: fieldsetClasses },
            this.label && h("legend", { class: labelClasses }, this.label),
            h("slot", null)));
    }
    static get is() { return "p-fieldset-wrapper"; }
    static get encapsulation() { return "shadow"; }
    static get originalStyleUrls() { return {
        "$": ["fieldset-wrapper.scss"]
    }; }
    static get styleUrls() { return {
        "$": ["fieldset-wrapper.css"]
    }; }
    static get properties() { return {
        "label": {
            "type": "string",
            "mutable": false,
            "complexType": {
                "original": "string",
                "resolved": "string",
                "references": {}
            },
            "required": false,
            "optional": true,
            "docs": {
                "tags": [],
                "text": "The label text."
            },
            "attribute": "label",
            "reflect": false,
            "defaultValue": "''"
        }
    }; }
}
