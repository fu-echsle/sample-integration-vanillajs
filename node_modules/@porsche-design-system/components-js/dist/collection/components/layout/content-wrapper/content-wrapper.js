import { Component, h, Prop } from '@stencil/core';
import { prefix } from '../../../utils';
export class ContentWrapper {
    constructor() {
        /** Defines the outer spacings between the content area and the left and right screen sides, as well as centering its content and setting a max-width. */
        this.width = 'basic';
    }
    render() {
        const contentWrapperClasses = {
            [prefix('content-wrapper')]: true,
            [prefix(`content-wrapper--${this.width}`)]: true
        };
        return (h("div", { class: contentWrapperClasses },
            h("slot", null)));
    }
    static get is() { return "p-content-wrapper"; }
    static get encapsulation() { return "shadow"; }
    static get originalStyleUrls() { return {
        "$": ["content-wrapper.scss"]
    }; }
    static get styleUrls() { return {
        "$": ["content-wrapper.css"]
    }; }
    static get properties() { return {
        "width": {
            "type": "string",
            "mutable": false,
            "complexType": {
                "original": "'basic' | 'extended' | 'fluid'",
                "resolved": "\"basic\" | \"extended\" | \"fluid\"",
                "references": {}
            },
            "required": false,
            "optional": true,
            "docs": {
                "tags": [],
                "text": "Defines the outer spacings between the content area and the left and right screen sides, as well as centering its content and setting a max-width."
            },
            "attribute": "width",
            "reflect": false,
            "defaultValue": "'basic'"
        }
    }; }
}
