import { ContentItem } from '../../services/state-tunnel';
export interface Series {
    PhraseId: string;
    Name: string;
    Image: string;
    Models: Model[];
    Further: ContentItem[];
    Meta: ContentItem[];
}
export interface Model {
    PhraseId: string;
    Name: string;
    Image: string;
    Link: string;
}
