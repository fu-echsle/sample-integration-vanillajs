import { Component, Element, h, Host, Prop, State } from '@stencil/core';
import cx from 'classnames';
import { mapBreakpointPropToPrefixedClasses, prefix, transitionListener, insertSlottedStyles } from '../../../utils';
export class TextareaWrapper {
    constructor() {
        /** The label text. */
        this.label = '';
        /** The description text. */
        this.description = '';
        /** The validation state. */
        this.state = 'none';
        /** The message styled depending on validation state. */
        this.message = '';
        /** Show or hide label. For better accessibility it is recommended to show the label. */
        this.hideLabel = false;
    }
    componentWillLoad() {
        this.setTextarea();
        this.setAriaAttributes();
        this.setState();
        this.bindStateListener();
        this.addSlottedStyles();
    }
    componentDidUpdate() {
        this.setAriaAttributes();
    }
    render() {
        const labelClasses = cx(prefix('textarea-wrapper__label'));
        const labelTextClasses = cx(prefix('textarea-wrapper__label-text'), mapBreakpointPropToPrefixedClasses('textarea-wrapper__label-text-', this.hideLabel, ['hidden', 'visible']), this.disabled && prefix('textarea-wrapper__label-text--disabled'));
        const descriptionTextClasses = cx(prefix('textarea-wrapper__description-text'), mapBreakpointPropToPrefixedClasses('textarea-wrapper__description-text-', this.hideLabel, ['hidden', 'visible']), this.disabled && prefix('textarea-wrapper__description-text--disabled'));
        const fakeTextareaClasses = cx(prefix('textarea-wrapper__fake-textarea'), prefix(`textarea-wrapper__fake-textarea--${this.state}`), this.disabled && prefix('textarea-wrapper__fake-textarea--disabled'), this.readonly && prefix('textarea-wrapper__fake-textarea--readonly'));
        const messageClasses = cx(prefix('textarea-wrapper__message'), prefix(`textarea-wrapper__message--${this.state}`));
        return (h(Host, null,
            h("label", { class: labelClasses },
                this.isLabelVisible &&
                    h("p-text", { class: labelTextClasses, color: 'inherit', tag: 'span', onClick: () => this.labelClick() }, this.label ? this.label : h("span", null,
                        h("slot", { name: 'label' }))),
                this.isDescriptionVisible &&
                    h("p-text", { class: descriptionTextClasses, tag: 'span', color: 'inherit', size: 'x-small', onClick: () => this.labelClick() }, this.description ? this.description : h("span", null,
                        h("slot", { name: 'description' }))),
                h("span", { class: fakeTextareaClasses },
                    h("slot", null))),
            this.isMessageVisible &&
                h("p-text", { class: messageClasses, color: 'inherit', role: this.state === 'error' && 'alert' }, this.message ? this.message : h("span", null,
                    h("slot", { name: 'message' })))));
    }
    get isLabelVisible() {
        return !!this.label || !!this.host.querySelector('[slot="label"]');
    }
    get isDescriptionVisible() {
        return !!this.description || !!this.host.querySelector('[slot="description"]');
    }
    get isMessageDefined() {
        return !!this.message || !!this.host.querySelector('[slot="message"]');
    }
    get isMessageVisible() {
        return ['success', 'error'].includes(this.state) && this.isMessageDefined;
    }
    setTextarea() {
        this.textarea = this.host.querySelector('textarea');
    }
    /*
     * This is a workaround to improve accessibility because the textarea and the label/description/message text are placed in different DOM.
     * Referencing ID's from outside the component is impossible because the web component’s DOM is separate.
     * We have to wait for full support of the Accessibility Object Model (AOM) to provide the relationship between shadow DOM and slots.
     */
    setAriaAttributes() {
        if (this.label && this.message) {
            this.textarea.setAttribute('aria-label', `${this.label}. ${this.message}`);
        }
        else if (this.label && this.description) {
            this.textarea.setAttribute('aria-label', `${this.label}. ${this.description}`);
        }
        else if (this.label) {
            this.textarea.setAttribute('aria-label', this.label);
        }
        if (this.state === 'error') {
            this.textarea.setAttribute('aria-invalid', 'true');
        }
        else {
            this.textarea.removeAttribute('aria-invalid');
        }
    }
    setState() {
        this.disabled = this.textarea.disabled;
        this.readonly = this.textarea.readOnly;
    }
    labelClick() {
        this.textarea.focus();
    }
    bindStateListener() {
        transitionListener(this.textarea, 'border-top-color', () => {
            this.setState();
        });
    }
    addSlottedStyles() {
        const tagName = this.host.tagName.toLowerCase();
        const style = `${tagName} a {
      outline: none transparent;
      color: inherit;
      text-decoration: underline;
      -webkit-transition: outline-color .24s ease, color .24s ease;
      transition: outline-color .24s ease, color .24s ease;
    }

    ${tagName} a:hover {
      color: #d5001c;
    }

    ${tagName} a:focus {
      outline: 2px solid #00d5b9;
      outline-offset: 1px;
    }
    `;
        insertSlottedStyles(this.host, style);
    }
    static get is() { return "p-textarea-wrapper"; }
    static get encapsulation() { return "shadow"; }
    static get originalStyleUrls() { return {
        "$": ["textarea-wrapper.scss"]
    }; }
    static get styleUrls() { return {
        "$": ["textarea-wrapper.css"]
    }; }
    static get properties() { return {
        "label": {
            "type": "string",
            "mutable": false,
            "complexType": {
                "original": "string",
                "resolved": "string",
                "references": {}
            },
            "required": false,
            "optional": true,
            "docs": {
                "tags": [],
                "text": "The label text."
            },
            "attribute": "label",
            "reflect": false,
            "defaultValue": "''"
        },
        "description": {
            "type": "string",
            "mutable": false,
            "complexType": {
                "original": "string",
                "resolved": "string",
                "references": {}
            },
            "required": false,
            "optional": true,
            "docs": {
                "tags": [],
                "text": "The description text."
            },
            "attribute": "description",
            "reflect": false,
            "defaultValue": "''"
        },
        "state": {
            "type": "string",
            "mutable": false,
            "complexType": {
                "original": "FormState",
                "resolved": "\"error\" | \"none\" | \"success\"",
                "references": {
                    "FormState": {
                        "location": "import",
                        "path": "../../../types"
                    }
                }
            },
            "required": false,
            "optional": true,
            "docs": {
                "tags": [],
                "text": "The validation state."
            },
            "attribute": "state",
            "reflect": false,
            "defaultValue": "'none'"
        },
        "message": {
            "type": "string",
            "mutable": false,
            "complexType": {
                "original": "string",
                "resolved": "string",
                "references": {}
            },
            "required": false,
            "optional": true,
            "docs": {
                "tags": [],
                "text": "The message styled depending on validation state."
            },
            "attribute": "message",
            "reflect": false,
            "defaultValue": "''"
        },
        "hideLabel": {
            "type": "any",
            "mutable": false,
            "complexType": {
                "original": "BreakpointCustomizable<boolean>",
                "resolved": "BreakpointValues<boolean> | boolean | string",
                "references": {
                    "BreakpointCustomizable": {
                        "location": "import",
                        "path": "../../../utils"
                    }
                }
            },
            "required": false,
            "optional": true,
            "docs": {
                "tags": [],
                "text": "Show or hide label. For better accessibility it is recommended to show the label."
            },
            "attribute": "hide-label",
            "reflect": false,
            "defaultValue": "false"
        }
    }; }
    static get states() { return {
        "disabled": {},
        "readonly": {}
    }; }
    static get elementRef() { return "host"; }
}
