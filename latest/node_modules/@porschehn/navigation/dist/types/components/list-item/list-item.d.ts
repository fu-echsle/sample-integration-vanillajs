import { JSX } from '../../stencil-public-runtime';
import { Item, LinkItem, TextItem } from '../entities/item';
export declare class ListItem {
    item: Item;
    sendNavigationClickAnalyticsEvent: (item: Item) => void;
    renderLinkItem(linkItem: LinkItem): JSX.Element;
    renderTextItem(textItem: TextItem): JSX.Element;
    render(): JSX.Element;
}
